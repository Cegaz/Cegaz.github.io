<!-- Modale sanction-->
<div class="modal" id="sanctionModal" tabindex="-1" role="dialog" aria-labelledby="doc" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
            </div>
            <div class="modal-body">
                {#TODO CG mettre en place formulaire #}
                {#{ form_start(form) }}#}
                {#<div class="form-group">#}
                    {#{{ form_row(form.type) }}#}
                {#</div>#}
                {#<div class="form-group">#}
                    {#{{ form_row(form.alertDate) }}#}
                {#</div>#}
                {#<div class="form-group">#}
                    {#{{ form_row(form.details) }}#}
                {#</div>#}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-success is-sanctionned">Save</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>